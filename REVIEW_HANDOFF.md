# Текущая передача: Distingt

Рабочее приложение: http://127.0.0.1:8766/ . Перенесён одобренный синий дизайн с локальным Manrope; добавлены сохранённые дела, структурированный демочат C010, контекст источников и неизменяемые Markdown-версии. Реальный `/api/analyze` сохранён. Серверный чат и применение правок не реализованы; их отсутствие явно обозначено. [Точный статус и проверка](docs/DISTINGT_WORKSPACE.md).

Следующие разделы — история прошлых итераций, не текущая спецификация оформления/хранения.

# Передача внешнему ревьюеру

## AYQYN frontend — текущая итерация

На 8766 работает новый композиционный лендинг и отдельная рабочая область. Главный экран принимает два комплекта (2–20 DOCX) через прежний /api/analyze; восемь учебных DOCX действительно обрабатываются. C010 — отдельное явно помеченное статическое демо с четырьмя исходными PDF, не ответ backend. Добавлены обзор, прямые hash-маршруты, справка/данные, независимые состояния демо/реального результата, область сравнения для потерь и полный keyboard/focus цикл источников. Backend/research контракты не менялись, только static allowlist.

[Проверки и ограничения текущего фронтенда](docs/AYQYN_FRONTEND_QA.md), [дизайн и происхождение материалов](docs/AYQYN_DESIGN.md). Финальный полный прогон: 182 теста, 1 пропуск, остальные прошли; перед ним наблюдались нестабильные прерывания отдельных research HTTP тестов. Основной браузерный путь проверен на реальном rules API, C010 проверен отдельно. Последующие разделы сохраняют историю прежних проверок; их упоминания двух-DOCX экрана и старого порта не описывают текущий frontend.


## Что проверять

Прототип по официальному кейсу HackAlem «Анализ организационной структуры и функционала». Запуск по README: http://127.0.0.1:8765/ (порт можно изменить). Основной экран принимает пакет 2–20 DOCX. Новый Agentic RAG и PDF подключены через отдельный серверный API/CLI, не через этот экран. [Схема](docs/research/agent-architecture.md), [приёмка новой итерации](docs/research/agentic-rag-evaluation.md).

## Проверенные результаты

- Последний полный автоматический прогон после синхронизации 2026-09-23: 182 теста, все прошли (Python 3.12.14, pypdf 6.10.0). Включены источники/таблицы, гибридный индекс с тестовыми векторами, native-протокол, сохранение/восстановление бюджета, отклонение цитат, исправление ошибок, остановки и HTTP-методы исследования. Это проверки кода, не смысловая приёмка R8/R9.
- Живой синтетический комплект с приложением: 10 реальных действий, 24,391 с; агент пересмотрел гипотезу потери на изменение владельца. Реальный embedding-поиск нашёл нужный переформулированный пункт на первом месте при нулевом лексическом совпадении.
- Тот же комплект без приложения: 12 действий, 26,875 с, `insufficient_data`, никаких подтверждённых потерь. Оба сохранённых запуска и двухминутный сценарий разбора указаны в отчёте итерации.
- На реальных редакциях 8/9 браузерный сценарий подтвердил появление ДИТААД и ДОА в результатах, открытие пункта 3.4 с дословным текстом, изменение статуса аналитиком и скачивание заключения.
- На отдельном синтетическом DOCX-комплекте HTTP-проверка нашла добавленное подразделение, возможную потерю формулировки и дублирование. Ответы не привязаны к именам файлов редакций 8/9.
- Интерфейс проверен на 1440×1000 и 390×844; мобильное переполнение исправлено, ошибок JavaScript нет.
- Дополнительный практикум: ошибка → объяснение → повтор → ещё два задания; итог 2/3 с первой попытки, 1/3 после объяснения, 4 попытки. Счётчики получены из действий.
- Три замечания независимого ревью исправлены: наследование ключа только для настроенного адреса; атомарная загрузка пары примеров; явная оценка даже при согласии с исходным статусом.

## Прежний Сценарий Интерфейса

1. Открыть редакции 8 и 9, нажать сравнение. Показать текущий режим обработки. Без подключённой модели это правила, не AI.
2. Отфильтровать подразделения. Открыть фрагмент ДИТААД/ДОА в пункте 3.4. Проверить название документа, текст и соседние абзацы.
3. Показать кандидаты потерь/дублирования и их ограничения. Отсутствие совпадения не доказывает потерю.
4. Дать оценку одному выводу. Скачать заключение и проверить соответствие статуса и источников.
5. Загрузить другие DOCX и убедиться, что результат вычисляется заново.

## Что ещё не доказано

Окончательный итог итерации: техническая часть и синтетический пересмотр гипотезы проверены, но R8/R9 не проходит смысловую приёмку. Повтор `r-5a6c95ba93324ab88cdfa7729c2112c5` обнаружил 0/5 эталонных изменений против 2/5 в первом новом запуске. Улучшение отдельных формулировок не компенсирует пропуски. [Окончательный отчёт](docs/research/agentic-rag-evaluation.md).

Первый Agentic RAG на R8/R9 (`r-97d443d7509247acb24903657d85ad42`, `gpt-5.4-2026-03-05`) механически завершился, но содержательно не прошёл: 2/5 изменений, 0/2 переформулировок, две ложные передачи и три необоснованных `created`. Все 53 цитаты существуют. Проверены все G01-G15 и девять находок: [независимый вердикт и сравнение повторного запуска](docs/research/agentic-rag-r8r9-verdict.md). Полное чтение обоих документов не обеспечило правильного сопоставления и потребовало много контекста. Прежний секционный эксперимент сохранён отдельно в [старом отчёте](docs/research/llm-evaluation.md).

Чтение текстового PDF, пакетный реестр, гибридный поиск, сохраняемое состояние и управляющий цикл подключены на сервере. Интеграция агентного API с интерфейсом, Excel и OCR отсутствуют. Текст PDF не означает визуальную проверку сложной вёрстки. Нет полного живого семантического прогона C001-C010. Сохранённый синтетический пример пересмотра пригоден для честного демо агентности; R8/R9 не является примером прошедшего приёмку анализа.

## Следующий обязательный шаг

Следующий приоритет: сократить повторную передачу служебных метаданных целых документов и проверить адресное сопоставление конкретных действий/владельцев с опровергающими свидетельствами. Не добавлять экраны и библиотеки до улучшения качества. Для каждого следующего изменения фиксировать до/после на том же контрольном наборе: обнаружение, достаточность источников, ложные тревоги, время и токены. Аннотации не подавать в рабочий запрос.

Критерий агентности: следующий запрос определяется найденным свидетельством и может изменить первоначальную гипотезу. Фиксированные последовательные запросы к модели, mock C010 и успешная проверка PDF этот критерий не выполняют. Отсутствие поискового результата не доказывает потерю.

## Интерфейс проверки источников

Реализовано рабочее место с двумя независимыми панелями, списком замечаний, фильтрами и инспектором решения. Контракт `/api/analyze`, парсер и ML-модуль не менялись. Выбор документа в панели влияет только на просмотр. Поддерживаются два загруженных DOCX, не библиотека из четырёх документов «орг/функции × до/после».

Комментарий хранится в `finding.note` только на клиенте и включается в отчёт вместе с оценкой. Ссылки остаются исходными `before_ids` / `after_ids`. При отсутствии ссылки совпавший номер раздела служит лишь контекстом и не подсвечивается как доказательство. Анимации отключаются при prefers-reduced-motion.

На этапе разработки этого интерфейса прошли 27 Python-тестов. Через браузер проверены реальные DOCX, панели, фильтры, диалог цитаты, решение и комментарий, экспорт Markdown и ширина 390 px. Результаты последующих живых AI-прогонов приведены выше.

## Корпоративный фронтенд и локализация

Новый frontend на том же vanilla JS/Python стеке: логотип Казахтелекома, light/dark/system, ru/kk/en, боковая навигация, четыре вкладки результатов, drawer источников, оценки и Markdown. Изменение backend ограничено явным списком публичных frontend-ресурсов в server.py; /api/analyze, парсер и модельный контракт сохранены.

Проверено: 29 Python-тестов, словари/настройки/контраст через scripts/check-frontend.cjs; браузерный реальный цикл и ошибки, fallback, 5 ширин в 2 темах, сохранение состояния при переключении языка. Обновлён scripts/check-ui.cjs; отдельно через CLI не запускался.

После следующего обновления AYQYN экран принимает пакеты DOCX, но агентный API с PDF, гибридным поиском и структурированными владельцами пока остаётся отдельным. Клиент не выдумывает эти данные. Качество R8/R9 не прошло приёмку; результаты живых запусков приведены выше.


## AYQYN packet workspace — 2026-09-23

- Identity: AYQYN by freax; blue/cyan/navy/white hero with original CSS glass-document composition, comparison preview and four-step timeline. Sources studied: KazPunct, freax_ds, freaxlab; see docs/AYQYN_DESIGN.md. No framework/runtime dependency added. Kazakhtelecom SVG unchanged.
- Unified 2–20 DOCX upload, 10 MB/file and 20 MB/packet. Automatic explicit filename labels or two versions/years of the same filename family. Optional model classification uses bounded front matter, validates document ID and exact supporting quote, never overrides explicit assignments. Unknowns return a review manifest before analysis. Manual side overrides supported.
- Existing endpoint supports both legacy pair and new documents array. All documents are aggregated into before/after collections for cross-document analysis. Every paragraph retains document_id/document_name/original_id and globally unique dN:pM. Existing limits apply per side; no silent text truncation for analysis.
- Individual documents selectable in either evidence panel; original download and report source filenames map to the selected file. Report includes per-file hashes. Completed backend milestones are shown in an expandable AgentTrace; no fabricated progress stages.
- Eight fictional example DOCX are marked in filenames and first paragraphs; generated by scripts/generate-demo-packet.py. Real original editions 8/9 are retained. Example results are computed, not canned.
- Fixed concrete rule defect found with the packet: verbal sentences containing ведет/формирует/направляет/согласовывает no longer become department names. Existing rule and model limitations remain.
- Validation: 39 Python tests passed; dictionary/preferences/contrast check: 240 keys × 3 languages; JS syntax and diff whitespace checks passed. Browser: eight-document rules analysis (6 candidates), unknown versions -> manual clarification -> successful retry, independent file selectors, full-source context, note/review persistence, report generation, three locales, both themes and responsive widths. Model classifications and provider responses are mocked in tests; live paid/model quality has not been verified.
- Browser CLI script updated to the packet flow, syntax checked only; UI run used Codex browser tools. Screenshots are external task outputs, not repository assets.

## Проверка после синхронизации — 2026-09-23

Подтянуты изменения до `4d19716`, сохранены локальные материалы `dataset/`, конфликт отчёта `llm-evaluation.md` разрешён с сохранением актуальной оценки команды. Для синтетического набора сверены 80 SHA-256 и прочитаны 45 JSON; входные файлы совпадают с `osnovanie_dataset_v1/`, поэтому копию нельзя считать вторым независимым набором. Локальные внешние PDF остаются вне Git согласно ограничениям их использования в `dataset/README.md`.

Проверки: `python -m unittest discover -s tests -v` — 182/182; `node scripts/check-frontend.cjs` — 240 ключей трёх языков, настройки и 12 пар контраста; `git diff --check` — без ошибок. PDF-тест научен искать уже имеющийся локальный отчёт в `dataset/real_sources/original_pdfs/`, прежний путь архива также поддерживается. В свежем клоне без этого необязательного PDF один тест будет пропущен. Браузерная проверка и новые живые запросы модели в этой синхронизации не выполнялись; прежние результаты выше относятся к работе команды.

Сопоставление с обязательным сценарием ТЗ:

| Требование | Текущее состояние и ограничение |
|---|---|
| Загрузка комплектов до/после | Основной экран: 2–20 DOCX; текстовый PDF — отдельный API/CLI; Excel отсутствует. |
| Изменения подразделений, потеря и дублирование функций | Правила и модель формируют кандидатов; качество на реальных R8/R9 не прошло смысловую приёмку по отчётам команды. |
| Потенциальный конфликт интересов | Предусмотрен модельным анализом, полнота и точность на контрольном комплекте не подтверждены. |
| Источники каждого существенного вывода | Есть адресация, исходные фрагменты и проверка существования цитат; достаточность доказательств проверяет аналитик. |
| Понятное итоговое заключение | Основной экран экспортирует Markdown с источниками и оценками; отдельный исследовательский агент пока не включён в этот интерфейс. |
| Демонстрация известных изменений | Есть DOCX-пример и C001–C010, но полный живой семантический прогон C001–C010 ещё не выполнен. |

Приоритет перед защитой: содержательная приёмка известных изменений с корректными источниками, затем подключение проверенного агентного пути к основному экрану. Успешные технические тесты сами по себе не закрывают смысловые требования ТЗ.
