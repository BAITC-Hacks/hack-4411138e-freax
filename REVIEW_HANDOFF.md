# Проверка интеграции frontend + backend + ML — 2026-09-23

Ветка `codex/fullstack-integration`: frontend main `1de0fb4`, модульный backend `c287a1e`, чат `7aa675f`. Один локальный Python-сервер раздаёт TeleDoc и API. [Сценарии и границы](docs/INTEGRATION.md). Изменения не отправлены на remote и не слиты в main.

- 296 Python-тестов прошли, включая 53 проверки чата. Восстановлена и проверена раздача JS/CSS, шрифтов, графики, сохранён запрет доступа к Python/секретам/output.
- JS-проверки: `check-frontend.cjs` (471 ключей, три языка), `check-agent.cjs`, `check-integration.cjs` прошли. Дополнительный интеграционный тест проверяет изоляцию демо, версию диалога, экранирование, повтор идентичного запроса после обрыва и отсутствие ключа в разметке.
- Реальный браузер/сервер: восемь учебных DOCX → шесть rules-кандидатов → дело → источники → оценка/комментарий → reload. Серверный analysis_id и история подключены. Без ключа чат сообщает о необходимости модели; исследование возвращает index_error, видимое ограничение и ноль выводов без заявления об отсутствии рисков.
- Desktop 1440×1000: ширина страницы 1430; mobile 390×844: 380, чат 344. В проверенных сценариях горизонтального переполнения нет. Источники остаются точными, DOCX не получает придуманную страницу. Настройки модели задаются через .env, как в актуальном frontend; поля ключа в UI нет.
- Отдельный QA-only сервер с подписанным offline stub проверил ответ, цитату через настоящий source endpoint, историю после reload. Это проверка UI/HTTP, не качества модели. Тестовый сервер и его данные находятся только в игнорируемом output, не входят в приложение.
- Живой провайдер не вызывался: ключ отсутствует. Предыдущая содержательная приёмка R8/R9 остаётся отрицательной. Серверная отмена чата, передача локальных оценок аналитика серверу и многопользовательская изоляция не реализованы.

- Windows: настоящий `run.ps1` создал свежую `.venv`, установил `pypdf==6.10.0` и запустил общий сервер; `/api/health` вернул `frontend: true`. Повторный запуск использовал готовое окружение без переустановки. `.env` создан из примера с пустым ключом, не входит в Git. DOCX ровно 100 МБ прошёл HTTP+парсер в тесте; превышение на один байт и лимит пакета проверены. Исследование сохраняет лимиты 10/40 МБ, чат — прежний лимит запросов. В актуальном TeleDoc повторно проверены переключение лимитов, анализ учебных DOCX и сообщение чата о настройке .env.

Ниже сохранён отчёт backend-команды до интеграции.

# Передача разработчику и ревьюеру

## Чат по анализу — 2026-09-23

Ветка `codex/backend-research-snapshot`, база `c287a1e`. Добавлены `ayqyn/chat/` и HTTP-адаптер `ayqyn/api/chat.py`. `/api/analyze` и терминальный research создают неизменяемые снимки; чат использует исходные документы, проверяет прочитанные точные цитаты, сохраняет историю и выполняет ограниченный цикл native tools через существующий провайдер. Новых зависимостей нет.

Контракт: [docs/api/analysis-chat-api.md](docs/api/analysis-chat-api.md). План и журнал синхронизации: [docs/plans/analysis-chat-backend.md](docs/plans/analysis-chat-backend.md). Интеграция актуального интерфейса из main ведётся отдельной задачей; этот коммит меняет только бэкенд, тесты и документацию.

Полный набор: **293 теста прошли** (WSL Python с уже установленным pypdf). Добавлены 53 теста: снимки pair/batch/research, изоляция источников, цитаты, охват, история, бюджеты, идемпотентность, параллельные вопросы, ошибки записи, рестарт, Host/Origin и полный HTTP → настоящий цикл агента со scripted provider. Живая смысловая проверка не выполнена: ключ модели в окружении не настроен. Проверки цитат подтверждают происхождение, но не логическую достаточность вывода.

Один процесс API должен владеть каталогом output/analyses. При перезапуске незавершённые ответы получают interrupted, без автоматического повторного расхода API. Состояние ручной оценки аналитика пока остаётся на фронтенде; чат явно не объявляет его известным.

## Запуск и документация — 2026-09-23

README переписан для первого знакомства: задача, архитектура, необходимые программы, .env, ручной запуск и работа API. run.sh/run.ps1 используют общий scripts/bootstrap.py: читают .env как данные, создают или переиспользуют .venv, устанавливают отсутствующие версии из requirements.txt и запускают сервер. Новых библиотек нет; .venv исключена из Git, run.sh закреплён с LF.

Проверены Bash, PowerShell 7 и Windows PowerShell 5.1: пути с пробелами, BOM/CRLF, аргументы, буквальные shell-подстановки, ошибки .env без раскрытия значений, повторное использование окружения. Дополнительно настоящий сервер запущен в чистом временном каталоге Linux: автоматически созданы .venv и установлен pypdf, /api/health вернул ожидаемый JSON. Повторный запуск не выполнял установку. Живая модель при этих проверках не вызывалась.

## Текущее состояние

По запросу пользователя проект переведён в бэкенд-структуру `ayqyn/`. Старые HTML/CSS/JS, runtime vendor и браузерные проверочные скрипты убраны. API, CLI, исходники, наборы данных и исследования сохранены.

- Запуск: `python server.py --port 8765` или `python -m ayqyn.api.server --port 8765`.
- `/` и `/api/health` возвращают JSON; страницы `/?research=...` больше нет.
- Результат: `/api/research/<id>?view=workspace`; источники — в `documents[].paragraphs`, оригинал — через `view=original`.
- [Запуск и архитектура](README.md), [контракт чата](docs/api/analysis-chat-api.md).

## Граница изменения

Изменены пути модулей, импорты, пути хеширования кода и статическая раздача. Не менялись промпт, поиск, embedding-модель, контракты сравнения и состояние исследований. Нормализация пробелов вынесена в `ayqyn/documents/text.py` с прежним поведением.

Перед изменением создан проверенный ZIP текущих локальных исходников, веб-файлов, тестов и документации: `output/backups/before-backend-layout-20260923T120624Z.zip`. Он содержит незакоммиченные правки команды и лицензии прежнего vendor. Архив локальный, игнорируется Git и не раздаётся API; указатель — `output/backups/latest-backend-layout.json`.

На этапе переноса remote refs не удалось обновить из-за ограничений окружения. В ходе реализации чата доступ восстановлен: upstream backend проверен, изменения main переданы отдельной задаче интеграции. Общая история не переписывалась.

## Проверки реорганизации

До переноса: **239 тестов прошли**. После переноса и добавления чата: **293 теста прошли**. Это техническая проверка; результаты смысловой оценки модели приведены отдельно ниже.

Сохраняются команды CLI, путь output/research, восстановление состояния, исходные source ID, цитаты, ошибки API и блокировка параллельного запуска одного исследования. Старые плоские Python-импорты заменены на пакетные; корневой server.py — только запуск.

## Последняя оценка модели — до переноса

`r-901c09ace68c4629960853a78caaf0ac`, `gpt-5.4-2026-03-05`, `reasoning_effort=none`, 24 обращения / 240 секунд. Сданы шесть записей без обрывов и дублей. G10 оформлен; G08 смешан с СВК, G09 частичный, G11/G12 пропущены. Полный независимый смысловой разбор не завершён. [Отчёт](docs/FINAL_REPORT.md).

Синтетический контроль ранее подтвердил пересмотр гипотезы после дополнительного источника. Он не доказывает качество на неизвестном комплекте. Браузерные проверки старых отчётов относятся к удалённому интерфейсу.

## Передача фронтендеру

Интерфейс использует реальные ответы API; просмотр сохранённого результата не делает POST. Сервер локальный, CORS для произвольного frontend origin отсутствует: подключение — с того же origin или через proxy. Сценарий «анализ сверху, чат ниже» и HTTP-контракт описаны в [API чата](docs/api/analysis-chat-api.md).

Различать прочитанное, проверенное агентом и подтверждённое аналитиком. Backend endpoints сохранения ручной оценки и экспорта отсутствуют: frontend должен реализовать их локально либо согласовать отдельное расширение API.

## Следующая работа по качеству

1. Диагностический тест выделения и сопоставления функций на небольшом достаточном контексте без эталонных ответов.
2. При фиксированном коде — отдельное сравнение настроек рассуждения.
3. Отдельная оценка исправления вытеснения семантических кандидатов RRF на сохранённых векторах.
4. Адресное чтение цитат, завершение начатых сопоставлений, затем новый сопоставимый R8/R9.

Не менять одновременно модель, парсер, поиск и поведение агента. Перестройка файлов не считается улучшением анализа.
